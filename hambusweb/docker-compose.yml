services:
  hambusweb:
    build:
      context: .
      dockerfile: Dockerfile
    image: hambusweb:latest
    container_name: hambusweb
    restart: unless-stopped
    ports:
      - "5180:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production

